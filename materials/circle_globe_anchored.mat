// SPDX-License-Identifier: Apache-2.0
//
// The circle family, bent onto a sphere by the anchored expansion -- plan.md §13.4's consumer half.
//
// `circle.mat` says what a circle is and how its quad is extruded; none of that changes on a
// sphere, because the extrusion is already in the space it belongs to. A circle standing up on
// the screen is extruded in *clip* space after the placement, and one lying on the ground is
// extruded in *tile units* before it -- so the only thing a sphere changes is the placement in
// between, which is what this file replaces.
//
// Without it a globe drew no circle layer at all: `materialFor` found nothing for the family and
// skipped the drawable.
material {
    name : tessella_circle_globe_anchored,
    shadingModel : unlit,
    blending : transparent,
    culling : none,
    constants : [
        // The permutation, as mbgl's `HAS_UNIFORM_u_*` defines but resolved when the material is
        // built. False is the uniform form, so a package built without specializing is the
        // constant-paint one.
        { name : colorFromAttribute,         type : bool, default : false },
        { name : radiusFromAttribute,        type : bool, default : false },
        { name : blurFromAttribute,          type : bool, default : false },
        { name : opacityFromAttribute,       type : bool, default : false },
        { name : strokeColorFromAttribute,   type : bool, default : false },
        { name : strokeWidthFromAttribute,   type : bool, default : false },
        { name : strokeOpacityFromAttribute, type : bool, default : false }
    ],
    parameters : [
        // `globe_ubo::GlobeBendUbo`, in declaration order, and the tile center it expands about.
        { type : float2, name : bendCenter },
        { type : float4, name : bendAnchor },
        { type : float4, name : bendU },
        { type : float4, name : bendV },
        { type : float4, name : bendUU },
        { type : float4, name : bendVV },
        { type : float4, name : bendUV },
        // Tile units per pixel, which is what turns a radius in pixels into an extrusion.
        { type : float2, name : extrudeScale },
        { type : float4, name : color },
        { type : float4, name : strokeColor },
        { type : float,  name : radius },
        { type : float,  name : blur },
        { type : float,  name : opacity },
        { type : float,  name : strokeWidth },
        { type : float,  name : strokeOpacity },
        // `circle-pitch-scale` and `circle-pitch-alignment`, as floats: a material parameter has
        // no bool. Both are false on a flat north-up map, which is the case the goldens pin.
        { type : float,  name : scaleWithMap },
        { type : float,  name : pitchWithMap },
        { type : float,  name : cameraToCenterDistance },
        { type : float,  name : pixelRatio },
        // The frame's zoom-mix factors, from `CircleDrawableUBO`.
        { type : float,  name : colorT },
        { type : float,  name : radiusT },
        { type : float,  name : blurT },
        { type : float,  name : opacityT },
        { type : float,  name : strokeColorT },
        { type : float,  name : strokeWidthT },
        { type : float,  name : strokeOpacityT }
    ],
    requires : [ position, custom0, custom1, custom2, custom3, custom4, custom5,
                 custom6 ],
    // Four, which is Filament's limit and is why the shape scalars share one: the fragment
    // needs the radius, the stroke width and the blur, and neither colour has room to spare.
    variables : [ circleData, circleFill, circleStroke, circleShape ]
}

vertex {
#include "circle_paint.glsl"

    // The expansion at a tile-local point, which is what replaces the placement matrix.
    vec4 bend(vec2 tileLocal) {
        vec2 d = tileLocal - materialParams.bendCenter;
        return materialParams.bendAnchor
             + materialParams.bendU * d.x
             + materialParams.bendV * d.y
             + 0.5 * (materialParams.bendUU * (d.x * d.x)
                    + materialParams.bendVV * (d.y * d.y))
             + materialParams.bendUV * (d.x * d.y);
    }

    void materialVertex(inout MaterialVertexInputs material) {
        // The corner sign rides in the low bit of each coordinate, the same trick the line vertex
        // uses: the point is doubled and zero or one added. So the center is the halved position
        // and the extrusion is what is left, mapped from {0,1} back to {-1,+1}.
        vec2 pos = getPosition().xy;
        vec2 extrude = mod(pos, 2.0) * 2.0 - 1.0;
        vec2 center = floor(pos * 0.5);

        float radius = resolveCircleRadius();
        float strokeWidth = resolveCircleStrokeWidth();
        float reach = radius + strokeWidth;
        vec4 clip;
        if (materialParams.pitchWithMap >= 0.5) {
            // Lying on the ground: the extrusion is in tile units and goes on before the matrix.
            vec2 corner = center;
            if (materialParams.scaleWithMap >= 0.5) {
                corner += extrude * reach * materialParams.extrudeScale;
            } else {
                // Held at a constant pixel size while lying flat, which needs the perspective
                // divide the matrix is about to do -- so it is measured first and multiplied in.
                vec4 projected = bend(center);
                corner += extrude * reach * materialParams.extrudeScale
                        * (projected.w / max(materialParams.cameraToCenterDistance, 1e-6));
            }
            clip = bend(corner);
        } else {
            // Standing up on the screen: the matrix places the center and the extrusion is added
            // in clip space afterwards, scaled by w so it survives the divide as a pixel size.
            clip = bend(center);
            if (materialParams.scaleWithMap >= 0.5) {
                clip.xy += extrude * reach * materialParams.extrudeScale
                         * materialParams.cameraToCenterDistance;
            } else {
                clip.xy += extrude * reach * materialParams.extrudeScale * clip.w;
            }
        }

        // The blur that hides the quad's own edge: one device pixel spread over the circle's
        // reach, so a small circle fades over more of itself than a large one.
        float antialias = 1.0 / max(materialParams.pixelRatio, 1e-6) / max(reach, 1e-6);
        material.circleData = vec4(extrude, antialias, 0.0);
        material.circleFill = resolveCircleColor() * resolveCircleOpacity();
        material.circleStroke = resolveCircleStrokeColor() * resolveCircleStrokeOpacity();
        material.circleShape = vec4(radius, strokeWidth, resolveCircleBlur(), reach);
        material.worldPosition = clip;
    }
}

fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);

        vec2 extrude = variable_circleData.xy;
        float antialias = variable_circleData.z;
        // The four the vertex resolved: the layer's paint, or this feature's.
        float radius = variable_circleShape.x;
        float strokeWidth = variable_circleShape.y;
        float blur = variable_circleShape.z;
        float reach = variable_circleShape.w;

        float extrudeLength = length(extrude);
        // Negative, because the ramp runs inwards from the quad's edge to the disc's.
        float edge = -max(blur, antialias);
        float inside = smoothstep(0.0, edge, extrudeLength - 1.0);
        // Where the fill gives way to the stroke. A hairline stroke is no stroke: below a
        // hundredth of a pixel the ratio below is noise, and mbgl cuts it off at the same place.
        float stroked = strokeWidth < 0.01
            ? 0.0
            : smoothstep(edge, 0.0, extrudeLength - radius / max(reach, 1e-6));

        material.baseColor = inside * mix(variable_circleFill, variable_circleStroke, stroked);
    }
}
